version: 2

models:
  - name: stg_stores
    columns: 
      - name: store_number
        tests:
          - unique
          - not null
  - name: stg_liquor_vendors
    columns:
      - name: vendor_id
        tests:
          - unique
          - not null
  - name: stg_liqour_categories
    columns:
      - name: category_id
        tests:
          - unique
          - not null
  - name: stg_location
    columns:
      - name: store_number
        tests:
          - unique
          - not null

